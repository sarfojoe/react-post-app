import React from "react";

const Header = (props) => (
  <header className="header">
    <h3 className="title">
      React<span>x</span>Firebase<span>x</span>Posts
    </h3>
  </header>
);

export default Header;
